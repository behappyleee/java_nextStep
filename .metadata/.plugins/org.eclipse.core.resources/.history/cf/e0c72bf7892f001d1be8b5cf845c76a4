package webserver;

import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStream;
import java.io.InputStreamReader;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class HttpRequest {
	
	Logger logger = LoggerFactory.getLogger(this.getClass());
	
	BufferedReader br;
	
	public HttpRequest(InputStream in) {
		try {
			InputStreamReader reader = new InputStreamReader(in);
	    	this.br = new BufferedReader(reader);
		} catch(Exception e) {
			e.printStackTrace();
		}
	}
	
	public String getMethod() {
		String line;
		
		
		
		return line.split(" ")[0];
	}
	
	public String getPath() {
		if(line.contains("?")) {
			return line.split(" ")[1].split("\\?")[0];
		} else {
			return line.split(" ")[1];
		}
	}
	
	public String getHeader(String selectHeader) {
		try {
			String readLine;
			while((readLine = br.readLine())!= null) {
				if(readLine.contains(selectHeader)) {
					return readLine.split(":")[1].trim(); 
				}
			}
			return null;
		} catch(Exception e) {
			e.printStackTrace();
		}
		return null;
	}
	
	public String getPrameter(String parameter) {
		try {
			
			
			
		} catch(Exception e) {
			e.printStackTrace();
		}
		return null;
	}
	
	
	
	public void selectInputStream(String selectLine) {
		
	};
	
	public void readAllInputStream() {
		InputStreamReader reader = new InputStreamReader(in);
    	BufferedReader br = new BufferedReader(reader);
		String line;
		
		try {
			while((line = br.readLine()) != null) {
				logger.debug("각 READ LINE : {} " , line);
			}
		} catch(Exception e) {
			e.printStackTrace();
		}
	}
	
}
