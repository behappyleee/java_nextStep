package webserver;

import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStream;
import java.io.InputStreamReader;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class HttpRequest {
	
	Logger logger = LoggerFactory.getLogger(this.getClass());
	
	InputStream in;
	
	String line;
	
	BufferedReader br;
	
	
	public HttpRequest(InputStream in) {
		this.in = in;
		try {
			InputStreamReader reader = new InputStreamReader(in);
	    	this.br = new BufferedReader(reader);
			String line;
			this.line = br.readLine();
			logger.debug("LINE CHECK : {} " , this.line);
		} catch(Exception e) {
			e.printStackTrace();
		}
	}
	
	public String getMethod() {
		logger.debug("GET METHOD : {} ",  line.split(" ")[0] );
		return line.split(" ")[0];
	}
	
	public String getPath() {
		if(line.contains("?")) {
			return line.split(" ")[1].split("\\?")[0];
		} else {
			return line.split(" ")[1];
		}
	}
	
	public String getHeader(String selectHeader) {
		
		try {
			String readLine;
			while((readLine = br.readLine())!= null) {
				logger.debug("BR READ LINE : {} ", readLine);
				if(readLine.contains(selectHeader)) {
					return readLine.split(":")[1]; 
				}
			}
			return null;
		} catch(Exception e) {
			e.printStackTrace();
		}
		
		return null;
	}
	
	
	
	
	public void selectInputStream(String selectLine) {
		
	};
	
	public void readAllInputStream() {
		InputStreamReader reader = new InputStreamReader(in);
    	BufferedReader br = new BufferedReader(reader);
		String line;
		
		try {
			while((line = br.readLine()) != null) {
				logger.debug("각 READ LINE : {} " , line);
			}
		} catch(Exception e) {
			e.printStackTrace();
		}
	}
	
}
